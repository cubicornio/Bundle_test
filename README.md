# Cubicornio Flask Bundle

Starter para crear **submódulos de Cubicornio** usando Flask.

## 1. Clonar e instalar

```bash
git clone https://github.com/tu-org/cubicornio-flask-bundle.git
cd cubicornio-flask-bundle
python -m venv venv
source venv/bin/activate  # en Windows: venv\Scripts\activate
pip install -r requirements.txt
